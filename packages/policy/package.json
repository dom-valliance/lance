{
  "name": "@lance/policy",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@lance/shared": "workspace:*",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "5.0.1",
    "fast-check": "^4.0.0",
    "@types/node": "26.6.2"
  }
}
