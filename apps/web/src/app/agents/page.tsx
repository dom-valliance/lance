import { PageHeading } from '@/components/page-heading';

export default function AgentsPage() {
  return (
    <PageHeading
      title="Agents"
      description="Every agent and watcher with its last run, age, cursor, breaker state, error tail, cost today and over seven days, and a token chart, alongside a global cost chart."
    />
  );
}
