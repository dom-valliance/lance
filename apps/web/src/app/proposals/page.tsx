import { PageHeading } from '@/components/page-heading';

export default function ProposalsPage() {
  return (
    <PageHeading
      title="Proposals"
      description="A queue of proposals with the same four actions available in Slack, filterable by cell, status and system, and a diff view for edited proposals."
    />
  );
}
