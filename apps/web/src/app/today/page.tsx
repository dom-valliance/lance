import { PageHeading } from '@/components/page-heading';

export default function TodayPage() {
  return (
    <PageHeading
      title="Today"
      description="A rendered morning brief with live state, a regenerate button, and an afternoon board that appears below it after 16:00."
    />
  );
}
