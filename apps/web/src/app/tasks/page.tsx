import { PageHeading } from '@/components/page-heading';

export default function TasksPage() {
  return (
    <PageHeading
      title="Tasks"
      description="Tasks aggregated across Notion, Jamie and Lance-native sources with source badges, task creation and completion routed through proposals except for Lance-native tasks, and Jamie tasks shown read-only with a link."
    />
  );
}
