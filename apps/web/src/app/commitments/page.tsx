import { PageHeading } from '@/components/page-heading';

export default function CommitmentsPage() {
  return (
    <PageHeading
      title="Commitments"
      description="Commitments I owe and commitments owed to me, each with ageing, a chase button, marking done, and dropping with a reason."
    />
  );
}
